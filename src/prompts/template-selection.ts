/**
 * designId 未指定時に list_templates からテンプレートを選ぶ手順（共通指示文）。
 *
 * generate_pdf / generate_pdfs の各レシピで同一の選択基準を提示するため、
 * 重複定義による不整合を避ける目的でここに集約する。
 */
export const TEMPLATE_SELECTION_INSTRUCTION =
  '1. `list_templates` を呼んでデザイン一覧を取得し、各デザインの `label`（テンプレート名）をユーザーの希望と照合してテンプレートを選びます。選択基準は次の優先順位に従うこと。\n' +
  '   - 最優先: `label`（名称）とユーザーが明示した希望の一致度。名称から明確に判断できる場合は、作業内容や文脈からの推測よりも名称の一致を優先すること。\n' +
  '   - 次点: 名称だけでは絞り込めない場合に限り、作業内容や文脈から最も適したテンプレートを推測してよい（推測自体は禁止しない）。\n' +
  '   - それでも候補が複数残る、または確証が持てない場合は、勝手に確定せず候補の `label` を提示してユーザーに選んでもらうこと。\n' +
  '   - 確定したテンプレートの `designId` と `latestVersion` を確認し、続けて `get_design_parameters` でパラメータ構造を取得します。';
